
spring:
  profiles:
    default: container

web-scrapping:
  scrapeme-shop:
      url: https://scrapeme.live/shop/

web-driver:
  pool:
    min-idle: 3
    max-idle: 6
    max-total: 12

logging:
  level:
    org:
      github:
        ehayik:
            kata:
              webscrapping: DEBUG
